// expressão de função

const soma = function(num1, num2) {
    return num1 + num2
}

console.log(soma(2,2))

// na expressão de função a função não tem um nome

//diferença principal função declarada são lidas primeiro
//expresão de função são lidas igual a const linha por linha.
//isso é chamado de hoisting.
